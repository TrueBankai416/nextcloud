# Apache reverse proxy configuration for additional services
# This file should be placed in /etc/apache2/conf-available/ and enabled

<IfModule mod_proxy.c>
    # Set proper headers for proxied requests
    ProxyPreserveHost On
    RequestHeader set X-Forwarded-Proto "https" env=HTTPS
</IfModule>
